var foodCost = 30;
var percent = 7;

var tipAmount = foodCost/percent;

var totalAmount = foodCost/percent + foodCost;

console.log("The tip on a $30 food bill is " + "$" + tipAmount.toFixed(2) + ".")

console.log("So the total is " + "$" + totalAmount.toFixed(2) + ".")